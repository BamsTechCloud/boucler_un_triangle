 
 let sortie = " ";
 let compteur = 0;
 while(compteur < 7) {
    sortie += "#";
    compteur += 1;
    console.log(sortie);
 }

 let sorties = " ";
 for(let compteur = 1 ; compteur <= 7 ; compteur += 1) {
     sorties += "#"
     console.log(sorties);
 }

 for(let diese = " #" ; diese.length <= 8; diese += "#") {
     console.log(diese);
 }